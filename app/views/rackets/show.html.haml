.container
  %h2 Add Strings
  = render 'stringsets/form' if @racket.stringsets.empty?
  / = @racket.stringsets.each { |stringset|link_to racket_path(stringset) } if @racket.stringsets

  = @racket.stringsets[0].brand if @racket.stringsets