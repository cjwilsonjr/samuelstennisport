.container
  %h2 Select #{@customer.first_name}  #{@customer.last_name}'s Racket
  - @rackets.each do |racket|
    = link_to racket.brand, racket_path(racket)
    <br>

  = link_to "Add Racket", new_customer_racket_path(@customer.id)
  / = render "rackets/form"
